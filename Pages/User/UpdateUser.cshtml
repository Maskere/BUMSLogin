@page
@model BUMS.UpdateUserModel
@{
    ViewData["Title"] = "Update User";
}
<h1>Edit User</h1>
<form method="post">
    <div class="form-group">
        <label asp-for="@Model.user.UserID" class="control-label">User ID</label>
        <input asp-for="@Model.user.UserID" class="form-control" readonly />
        <span asp-validation-for="@Model.user.UserID" class="text-danger"></span>
    </div>

  @*  <div>
        <label asp-for="@Model.user.UserName" class="control-label">Varekategori</label>
        <select asp-for="@Model.user.UserName" asp-items="@Html.GetEnumSelectList<UserGroup>()">
            <option selected="selected" value="">Vælg et kategori</option>
        </select>
        <span asp-validation-for="@Model.item.StorageType" class="text-danger"></span>
    </div> *@

    <div class="form-group">
        <label asp-for="@Model.user.UserName" class="control-label">Username</label>
        <input asp-for="@Model.user.UserName" class="form-control" />
        <span asp-validation-for="@Model.user.UserName" class="text-danger"></span>
    </div>
    <p></p>
    <div class="form-group">
        <input type="submit" value="Submit" class="btn btn-primary" />
    </div>
    <div>
        <a asp-page="GetUser">Back to users</a>
    </div>
</form>